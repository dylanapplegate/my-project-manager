{
  "tasks": {
    "dev": "deno run --watch src/cli.ts",
    "migrate": "deno run -A npm:prisma migrate dev --schema=prisma/schema.prisma",
    "studio": "deno run -A npm:prisma studio --schema=prisma/schema.prisma",
    "generate": "deno run -A npm:prisma generate --schema=prisma/schema.prisma"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "dotenv-cli": "npm:dotenv-cli@^8.0.0"
  }
}
